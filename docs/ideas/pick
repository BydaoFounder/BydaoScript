pick = "pick" expr ["as" var_name]
    "when" expr {"," expr } "{" block "}"
    { "when" expr {"," expr } "{" block "}" }
    [ "else" "{" block "}"

Пример:

pick data['type'] as type
    when Obj.type1 {
        // use type in expressions
    }
    when Obj.type1 {
    }
    else {
    }

pick Int.range(1,10) % 2
    when 0 {
    }
    when 1 {
    }
