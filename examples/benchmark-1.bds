use Sys

const cnt = 1000000
const one = 1
var t1 = Sys.time()
var i = 0
var sum = 0
while i < 1000000 next i = i + 1 {
    sum = sum + i
}
t1 = Sys.time() - t1
Sys.outln( 'time: ' + t1 + ' msec, sum = ' + sum )

t1 = Sys.time()
i = 0
sum = 0
while i < cnt next i += one {
    sum += i
}
t1 = Sys.time() - t1
Sys.outln( 'time: ' + t1 + ' msec, sum = ' + sum )
